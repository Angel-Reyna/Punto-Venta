import request from "supertest"; import { app } from "../src/app"; describe('health',()=>{ it('returns ok',async()=>{ const r=await request(app).get('/health'); expect(r.status).toBe(200); expect(r.body.status).toBe('ok'); }); });
