import { NextFunction, Request, Response } from "express"; import jwt from "jsonwebtoken"; import { Role } from "@prisma/client"; import { env } from "../config/env"; import { AppError } from "../utils/AppError"; import { AuthUser } from "../types";
export function requireAuth(req:Request,res:Response,next:NextFunction){ const header=req.headers.authorization; if(!header?.startsWith("Bearer ")) throw new AppError(401,"Token requerido"); try{ req.user=jwt.verify(header.replace("Bearer ",""), env.JWT_ACCESS_SECRET) as AuthUser; next(); }catch{ throw new AppError(401,"Token inválido o expirado"); } }
export function requireRole(...roles:Role[]){ return (req:Request,res:Response,next:NextFunction)=>{ if(!req.user) throw new AppError(401,"No autenticado"); if(!roles.includes(req.user.role)) throw new AppError(403,"No autorizado"); next(); }; }
