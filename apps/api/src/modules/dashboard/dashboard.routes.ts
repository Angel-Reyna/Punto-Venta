import { Router } from "express"; import { prisma } from "../../config/prisma"; import { requireAuth } from "../../middlewares/auth"; import { asyncHandler } from "../../utils/asyncHandler";
export const dashboardRouter=Router(); dashboardRouter.use(requireAuth); dashboardRouter.get('/',asyncHandler(async(_req,res)=>{const todayStart=new Date(); todayStart.setHours(0,0,0,0); const [products,lowStock,todaySales,users]=await Promise.all([prisma.product.count({where:{isActive:true}}),prisma.product.count({where:{isActive:true,stock:{lte:5}}}),prisma.sale.findMany({where:{createdAt:{gte:todayStart}}}),prisma.user.count({where:{isActive:true}})]); res.json({products,lowStock,users,todaySalesCount:todaySales.length,todaySalesTotal:todaySales.reduce((sum,sale)=>sum+Number(sale.total),0)});}));
